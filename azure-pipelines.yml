# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  name: Default  
  demands:
    - Agent.Name -equals VALE  

variables:
  buildConfiguration: 'Release'

jobs:
- job: BuildAndTest
  displayName: 'Compilar y probar en agente Windows'
  steps:
  - checkout: self





